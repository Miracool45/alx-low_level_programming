#include "main.h"

/**
 * swap_int - function that swaps the values of two integers.
 * @a: input of value that swaps two integers.
 * @b: input of value that swaps two integers.
 */
void swap_int(int *a, int *b);
{
	*a = 98;
	*b = 42;
}
